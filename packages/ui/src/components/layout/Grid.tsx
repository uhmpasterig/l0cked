import { Cross as GridCross } from "./Grid.Cross";
import { Box as GridBox } from "./Grid.Box";
import { Item as GridItem } from "./Grid.Item";

export namespace Grid {
  export const Box = GridBox;
  export const Item = GridItem;
  export const Cross = GridCross;
}